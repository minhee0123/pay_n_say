import 'package:flutter/material.dart';

class EnglishLearningPage extends StatelessWidget {
  const EnglishLearningPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('영어 학습'),
      ),
      body: const Center(
        child: Text('영어 학습 화면 (준비 중)'),
      ),
    );
  }
}
